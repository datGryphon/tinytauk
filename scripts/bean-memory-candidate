#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PROFILE="${PROFILE:-profiles/bean-memory-candidate.toml}"
export OUT_DIR="${OUT_DIR:-benchmarks/results/bean-memory-candidate}"
exec bash "$ROOT/scripts/bean-candidate"
